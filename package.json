{
  "name": "spur-gear-mobile-app",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile app wrapper for the Spur Gear Calculator and PDF report generator.",
  "scripts": {
    "serve": "npx http-server www -p 8080",
    "sync": "npx cap sync",
    "android": "npx cap add android && npx cap open android",
    "ios": "npx cap add ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest",
    "@capacitor/ios": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "http-server": "latest"
  }
}
